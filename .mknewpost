:
test $# -eq 0 && {
    echo "Usage: $0 [post name]"
    exit 1
}

name=$(echo $* | sed -e 's/ /-/g')

hugo new posts/${name}.md

# todo this better
echo

filename=content/posts/$(ls -t content/posts | sed 1q)
# echo $filename


# filename=content/posts/parasocial-relationship.md

echo "New file is: $filename"
echo -n "Edit? (Y/n):"
read yn
case "$yn" in
  (n|N|no|NO) : exit 0 ;;
  (*) exec vim $filename ;;
esac

exit 0
